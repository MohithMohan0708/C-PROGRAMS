#include<stdio.h>
int main()
{
int n,i,key,count=0;
printf("enter the number of numbers\n");
scanf("%d",&n);
int a[n],b[n];
for(i=1;i<=n;i++)
{
printf("enter %d th number",i);
scanf("%d",&a[i]);
}
for(i=1;i<=n;i++)
{
if(a[i]==key)
{
count+=1;
printf("%d is found at %d at position %d)
